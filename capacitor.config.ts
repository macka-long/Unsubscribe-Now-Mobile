import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'unsubscribe-now',
  webDir: 'dist',
  ios: { contentInset: 'always' },
  backgroundColor: 'black',
};

export default config;
